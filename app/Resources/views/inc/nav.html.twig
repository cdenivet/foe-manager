<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path('homepage.index') }}">
        <img src="{{ asset('img/foe_logo.png') }}" alt="" width="100" height="50">
    </a>
    <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage.index') }}"><span class="fas fa-home text-"></span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                    Eres
                </a>
                <div class="dropdown-menu">
                    {% for era in eras %}
                        <a class="dropdown-item" href="{{ path('era.index', { 'slugEra' : era.slug }) }}">{{ era.name }}</a>
                    {% endfor %}
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                    GM
                </a>
                <div class="dropdown-menu">
                    {% for gm in gms %}
                        <a class="dropdown-item" href="{{ path('gm.index', { 'slugGm' : gm.slug }) }}">{{ gm.name }}</a>
                    {% endfor %}
                </div>
            </li>
        </ul>
        <div class="my-1">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                        <i class="fa fa-globe" aria-hidden="true"></i>

                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="/fr/">{{ 'French' | trans }}</a>
                        <a class="dropdown-item" href="/en/">{{ 'English' | trans }}</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <span class="fas fa-user text-info"></span>
                        {% else %}
                            <span class="fas fa-user"></span>
                        {% endif %}
                    </a>
                    <div class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="dropdown-item text-info" href="{{ path('admin.homepage.index') }}">Tableau de bord</a>
                        {% endif %}
                        {% if is_granted('ROLE_USER') %}
                            <a class="dropdown-item text-info" href="{{ path('profile.homepage.index') }}">Mon compte</a>
                        {% endif %}
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <a class="dropdown-item text-danger" href="{{ path('security.logout') }}">Déconnexion</a>
                        {% else %}
                            <a class="dropdown-item text-primary" href="{{ path('security.login') }}">Connexion</a>
                            <a class="dropdown-item text-primary" href="{{ path('account.signup') }}">Inscription</a>
                        {% endif %}
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>